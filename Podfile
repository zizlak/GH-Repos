# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GH-Repos' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GH-Repos
  pod 'SwiftLint'

  target 'GH-ReposTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GH-ReposUITests' do
    # Pods for testing
  end

end
